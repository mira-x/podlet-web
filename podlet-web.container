# podlet-web.container

# This file was converted from the docker-compose! (Although the "build: ." part had to be removed)

# To build this container, run: buildah build

[Container]
Environment=ROCKET_ADDRESS=0.0.0.0 ROCKET_PORT=80
HealthCmd=wget --no-verbose --tries=1 --spider http://localhost:8080/ || exit 1
HealthInterval=30s
HealthRetries=3
HealthStartPeriod=30s
HealthTimeout=10s
Image=localhost/podlet-web
PublishPort=8080:80

[Service]
Restart=always
